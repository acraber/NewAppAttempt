module.exports = {
    extends: ['handlebarlabs'],
    rules: {
        'react/jsk-props-no-spreading': 0,
        'react/jsk-curly-newline': 0,
    }
    // 2 import lint and using yarn add --dev eslint eslint-config-handlebarlabs then add thees dependencies.
    // also add prettier using yarn add --dev prettier 
}